name: fling_gallery
description: "A Flutter layout widget that positions its children in tight rows."
version: 1.0.3
homepage: https://fling-gallery-demo.web.app/
repository: https://github.com/mongoose13/fling-gallery
issue_tracker: https://github.com/mongoose13/fling-gallery/issues
funding:
  - https://www.paypal.com/paypalme/BruceSantier
topics:
  - gallery
  - image
  - photo
  - photos

environment:
  sdk: '>=3.0.0 < 4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
